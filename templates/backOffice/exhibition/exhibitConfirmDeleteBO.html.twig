{% extends 'base.html.twig' %}

{% block title %}Suppression d'une Exposition{% endblock %}

{% block description %}
    Cette page vous permet de confirmer la suppression d'une exposition. Si vous confirmez, l'exposition ainsi que tous les artistes associés seront définitivement supprimés.
{% endblock %}



{% block body %}
<!-- BackOffice/ExhibitionController -->

<div class="containH1">    
    <h1>Expositions</h1>
</div>

<section>
    <p>blabla</p>
    
    {% if app.user %}
        {% if 'ROLE_ADMIN' in app.user.roles %} 
            <p class="marginTop textDelete">L'exposition <strong>{{ exhibition }}</strong> sera entièrement supprimée ainsi que les artistes programmés.</p>
            
            <p class="marginTop textDelete">Etes vous sûr de vouloir continuer ?</p>

            <div class="button">
                <a class="btnSubmit" href="{{ path('deleteConfirmExhibitBO', {'slugExhibit': exhibition.slug}) }}">
                    Supprimer
                </a>
            </div>
        {% endif %}
    {% endif %}
</section>
{% endblock %}


