{% extends 'base.html.twig' %}

{% block title %}Hello ! {% endblock %}



{% block body %}  

<section>     
    {% for exhibition in exhibitions %}
    <h2>{{ exhibition.titleExhibit }}</h2>
    <p>{{ exhibition.dateExhibit|date('d/m/Y') }}</p>
    {% endfor %}

    <p>Salut ceci est le super accueil de mon site !</p>
</section> 

{% endblock %}

